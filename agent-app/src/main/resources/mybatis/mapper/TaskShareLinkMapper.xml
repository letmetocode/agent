<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.getoffer.infrastructure.dao.TaskShareLinkDao">

    <resultMap id="BaseResultMap" type="com.getoffer.infrastructure.dao.po.TaskShareLinkPO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="task_id" property="taskId" jdbcType="BIGINT"/>
        <result column="share_code" property="shareCode" jdbcType="VARCHAR"/>
        <result column="token_hash" property="tokenHash" jdbcType="VARCHAR"/>
        <result column="scope" property="scope" jdbcType="VARCHAR"/>
        <result column="expires_at" property="expiresAt" jdbcType="TIMESTAMP"/>
        <result column="revoked" property="revoked" jdbcType="BOOLEAN"/>
        <result column="revoked_at" property="revokedAt" jdbcType="TIMESTAMP"/>
        <result column="revoked_reason" property="revokedReason" jdbcType="VARCHAR"/>
        <result column="created_by" property="createdBy" jdbcType="VARCHAR"/>
        <result column="version" property="version" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, task_id, share_code, token_hash, scope, expires_at,
        revoked, revoked_at, revoked_reason, created_by, version, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.getoffer.infrastructure.dao.po.TaskShareLinkPO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task_share_links (
            task_id, share_code, token_hash, scope, expires_at,
            revoked, revoked_at, revoked_reason, created_by, version, created_at, updated_at
        ) VALUES (
            #{taskId}, #{shareCode}, #{tokenHash}, #{scope}, #{expiresAt},
            #{revoked}, #{revokedAt}, #{revokedReason}, #{createdBy},
            COALESCE(#{version}, 0), CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
        )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_share_links
        WHERE id = #{id}
    </select>

    <select id="selectByTaskIdAndShareCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_share_links
        WHERE task_id = #{taskId}
          AND share_code = #{shareCode}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectByTaskId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_share_links
        WHERE task_id = #{taskId}
        ORDER BY created_at DESC
    </select>

    <update id="revokeById">
        UPDATE task_share_links
        SET revoked = TRUE,
            revoked_at = CURRENT_TIMESTAMP,
            revoked_reason = #{revokedReason},
            updated_at = CURRENT_TIMESTAMP,
            version = version + 1
        WHERE task_id = #{taskId}
          AND id = #{id}
          AND revoked = FALSE
    </update>

    <update id="revokeAllByTaskId">
        UPDATE task_share_links
        SET revoked = TRUE,
            revoked_at = CURRENT_TIMESTAMP,
            revoked_reason = #{revokedReason},
            updated_at = CURRENT_TIMESTAMP,
            version = version + 1
        WHERE task_id = #{taskId}
          AND revoked = FALSE
    </update>

</mapper>
